import { ReactElement } from 'react';
export interface PopoverProps {
    children?: string;
    className?: string;
}
declare const Popover: ({ className, children, }: PopoverProps) => ReactElement;
export { Popover };
